{
    "version": "0.2.0",
    "configurations": [   
        {
            "name": "Debug Harvester Functions",
            "type": "coreclr",
            "request": "attach",
            "processId": "${command:azureFunctions.pickProcess}"
        }
    ]
}